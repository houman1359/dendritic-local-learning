"""Analyses of transformer-layer replacement screens.

This subpackage hosts post-hoc statistical analyses that operate on the
on-disk artifacts of replacement screens (per-arm ``benchmark_metrics.json``
and ``metrics.json`` files under a screen root), not on live training state.
"""
