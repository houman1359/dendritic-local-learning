# Reviewer software validation

The draft staging archive passes policy, checksum, ZIP membership and CRC checks. It is 10,246,492 bytes with 1,754 members, including 534 implementation files. Presentations, internal revision logs and unrelated project drivers are absent. Two text-artifact reader modules and their initializer remain because the shared training API imports them.

A virtual environment without system site-packages installed the staged package as a wheel. The clean smoke imports branch dynamics and the training entry point, follows the installed core into the released noise adapter, rejects an ambiguous historical task key, checks flattened legacy inputs and applies the frozen projected-noise transformation to tiny tensor fixtures. All checks passed, as did all 11 focused packaging and generator tests. `pip check` reports no broken requirements. `validated_environment.txt` records the resolved environment; `VALIDATION.json` records the artifact hash and scope.

This validation concerns a clearly marked draft package assembled before the scientific integration commit. The final software archive must still be built from the committed paper and checked again. No raw biological assets, upstream image downloads or complete historical training trajectories are validated by this smoke. Cohort-to-generator provenance gaps remain explicit in `task_identity.json`.
