# Reviewer software validation

The v7 draft staging archive passes policy, checksum, ZIP membership and CRC checks. It is 12,025,692 bytes with 2,261 members, including 534 current implementation files and 476 historical physical-depth source files. Presentations, internal revision logs and unrelated project drivers are absent. Two text-artifact reader modules and their initializer remain because the shared training API imports them. `VALIDATION.json` records this preview's exact archive hash and distinguishes it from the earlier clean-install preview.

A virtual environment without system site-packages installed the staged core as a wheel. The clean smoke imports branch dynamics and the training entry point, follows the installed core into the released noise adapter, rejects an ambiguous historical task key, checks flattened legacy inputs and applies the frozen projected-noise transformation to tiny tensor fixtures. All checks passed, as did all 11 focused packaging and generator tests. `pip check` reports no broken requirements. `validated_environment.txt` records the resolved environment.

The historical export at `a99c3a777f99913e13dfe673a3f3a28bfe3566af` verifies all 476 selected source files against their original hashes and declared release transformations. The portable launcher imports this runtime in a fresh process. Both an exact-gradient condition and a LocalCA/path-transport condition passed a one-epoch smoke in the same clean environment. These short runs are labeled as excluded smoke checks and add no scientific observations; the frozen 600-epoch runner remains unchanged.

The documented copied-paper layout verifies all 684 software identities, including 22 transformed files, through an explicit relocation record. Separate tests verify that restoring Source Data retains the software chain, that complete tables take precedence over display subsets, and that tampering with the relocation record is rejected. All 11 current packaging and restoration tests passed.

This validation concerns clearly marked draft packages assembled before the scientific integration commit. The final software archive must still be built from the committed paper and checked again. No raw biological assets, upstream image downloads or complete historical training trajectories are validated by these smokes. Cohort-to-generator provenance gaps remain explicit in `task_identity.json`.
