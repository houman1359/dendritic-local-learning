{
  "study": "local_context_coefficient_encoder",
  "status": "protocol_fixed_before_new_training_outcomes_not_public_preregistration",
  "seeds": [52000,52001,52002,52003,52004,52005,52006,52007,52008,52009,52010,52011,52012,52013,52014,52015,52016,52017,52018,52019],
  "canary_seed": 51999,
  "calibration_samples": [16,64,256],
  "cue_noise_sd": [0.0,0.5,1.0],
  "cue_delay_trials": [0,1,4],
  "methods": ["oracle_context", "learned_local_cue", "frozen_profile", "mismatched_encoder"],
  "encoder": {"type": "four_output_softmax", "epochs": 30, "batch_size": 16, "learning_rate": 0.2, "weight_decay": 0.001},
  "task_training": "original_eight_context_task_full_batch_80_epochs",
  "route_channels": 4,
  "checkpoints": [0,1,10,40,80],
  "primary_condition": {"calibration_samples":256,"cue_noise_sd":0.5,"cue_delay_trials":0},
  "primary_endpoint": "heldout_accuracy",
  "primary_contrasts": ["learned_local_cue_minus_oracle_context", "learned_local_cue_minus_frozen_profile", "learned_local_cue_minus_mismatched_encoder"],
  "bootstrap_draws": 20000,
  "scope": "An imposed local branch-context cue is available during forward activation. The encoder learns its mapping to four subtree coefficients using local branch activation targets on separate calibration trials. It never receives exact parameter gradients or held-out task labels. Its weights are frozen before task training. Trial delay is a cue/eligibility mismatch under independent contexts, not a calibrated physiological time constant."
}
