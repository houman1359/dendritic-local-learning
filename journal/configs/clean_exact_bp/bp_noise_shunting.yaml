extends: ../prospective_learning_benefits/confirmatory_noise_shunting_backprop.yaml
output_dir: drafts/dendritic-local-learning/journal/analysis/clean_exact_bp/generated
execution_results_dir: drafts/dendritic-local-learning/journal/analysis/clean_exact_bp/results/bp_noise_shunting
slurm_config:
  max_concurrent_jobs: 8
  mem: 80GB
sweep_contract:
  expected_config_count: 40
base_config:
  outputs:
    run_name: clean_bp_noise_shunting
  model:
    core:
      transfer:
        # Positive-conductance shunting requires non-negative conductance drives.
        # The synthetic task is signed, so rectify the shared transfer output.
        output_activation: relu
