extends: ../reruns/feedback_definition_additive_15seed.yaml

# Current-source replication of the scalar and neuron-specific conditions so
# all three Figure 2A rungs share one source commit and execution environment.
output_dir: /n/holylfs06/LABS/kempner_project_b/Lab/dendritic/HS/LOCAL_LEARNING/journal_extension_20260820/sweep_runs/mnist_feedback_ladder

slurm_config:
  account: kempner_bsabatini_lab
  partition: kempner_h100_priority
  time: "01:00:00"
  max_concurrent_jobs: 8

experiment_settings:
  seeds_per_condition: 15
  base_seed: 42

sweep_contract:
  expected_config_count: 30

sweep_config:
  training.main.learning_strategy_config.error_broadcast_mode:
    - per_soma
    - per_soma_shared

base_config:
  wandb:
    use_wandb: false
  outputs:
    run_name: journal_mnist_feedback_ladder_coordinates_additive_15seed

