extends: ../reruns/feedback_definition_additive_15seed.yaml

# Decisive strict-scalar control for Figure 2A. This differs from the matched
# current-source coordinate cohort only in forcing one signed scalar error per
# example at every local-rule stage.
output_dir: /n/holylfs06/LABS/kempner_project_b/Lab/dendritic/HS/LOCAL_LEARNING/journal_extension_20260820/sweep_runs/mnist_feedback_ladder

slurm_config:
  account: kempner_bsabatini_lab
  partition: kempner_h100_priority
  time: "01:00:00"
  max_concurrent_jobs: 8

experiment_settings:
  seeds_per_condition: 15
  base_seed: 42

sweep_contract:
  expected_config_count: 15

sweep_config:
  training.main.learning_strategy_config.error_broadcast_mode:
    - scalar

base_config:
  wandb:
    use_wandb: false
  outputs:
    run_name: journal_mnist_feedback_ladder_strict_scalar_additive_15seed

