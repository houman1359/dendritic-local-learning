\phantomsection
\section*{Notation guide}
\addcontentsline{toc}{section}{Notation guide}

\begin{table}[!htbp]
\centering
\caption{Core notation. Each dictionary and gradient is defined in its stated coordinate system; the same tree can support different delivery and update representations. Symbols specific to an experiment are defined in its section.}
\label{tab:notation}
\small
\begin{tabularx}{\textwidth}{p{0.24\textwidth}X}
\toprule
Symbol & Meaning \\
\midrule
$V_n$, $a_n=f_n(V_n)$ & Compartment voltage and transmitted activity. \\
$g_i,x_i,g_ix_i$; $E_i^{\rm rev}$ & Maximal conductance, presynaptic activity, open conductance and reversal potential. Excitation and inhibition differ through driving forces, not signed conductances. \\
$g_n^{\rm tot},R_n^{\rm tot}$ & Total open conductance and reciprocal local resistance, holding incoming child outputs fixed in a directed tree; not the driving-point resistance of a reciprocal cable. \\
$\delta^V_n$, $\delta^a_n$; $q_n$ & Loss derivatives in voltage/activity coordinates. In interaction-task diagnostics, $q_n=\partial u_0/\partial u_n$; in the implicit/cable analysis, the adjoint satisfies $J_V^{\mathsf T}\bm q=\nabla_{\bm V}\mathcal L$. \\
$A_u$, $A_T$ & Compartmental delivery matrix and anatomical support lifted to synaptic coordinates, respectively (Eqs.~\ref{eq:s_routed_field}, \ref{eq:s_dictionary}). \\
$\Phi_{T,K}$ & Synaptic-update dictionary after eligibility or anatomical weighting, route gains and channel selection: $D_eA_T\operatorname{diag}(\bm\beta)S_K$. Distinct from the compartmental matrix $A_u$. \\
$h_{up}$; $\bm h^{(0)}$ & Relative-resistance gate in the directed population; baseline current-to-soma transfer in the reciprocal cable. \\
$K$, $\bm c$; $\Gamma$ & Profile budget, delivered coefficients and diagonal delivery gains. Rank may be smaller than $K$. \\
$W$, $P_{\Phi,W}$; $\mathcal C_K$ & Positive coordinate weights, weighted projector and specified per-field or aggregate energy capture. The anatomical kernel in Eq.~\ref{eq:s_kernel} is separately defined. \\
$C$, $C_0$, $C_{\rm res}$ & Total, broadcast and residual capture for one cell: $C_{\rm res}=(C-C_0)/(1-C_0)$. Spatial share $C-C_0$ instead uses total energy as its denominator. \\
$\bm g$, $\bm d$, $\bm m$, $V$ & Exact gradient, routed-update estimator, its conditional mean and covariance in the moment bound. \\
$M$, $\Sigma$, $U$, $\eta^*$ & Fixed routing operator, estimator-noise covariance, certified one-step decrease and bound-optimal nonnegative step. \\
$d_i=E_{\rm E}-V_i$, $\gamma_i=q_id_i$; $\Lambda_k$ & Local driving force, excitatory-conductance gradient and localization index in the focal-shunt analysis; these are distinct from the routed-update vector $\bm d$ above. \\
$H_{\rm c},D_{\rm r}$; $H_{\rm p},D_{\rm p}$ & Task-credit hierarchy/routed resolution, and latent task hierarchy/physical stage count. Bracketed morphologies are specified separately for each cohort. \\
$\chi$, $\alpha$, $a$ & Branch conflict, physical-task sensor alignment and imposed route-span energy fraction, respectively. \\
$G$, $R_a$, $R_m$, $\kappa$ & Reciprocal conductance matrix, axial resistivity, specific membrane resistance and focal shunt dose. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection*{Reading conventions across experiments}

Bold symbols are vectors or matrices; $\mathsf T$ denotes transpose,
$\|\cdot\|$ the Euclidean norm, $\|\cdot\|_F$ the matrix Frobenius norm,
and $\mathbf1[\cdot]$ an indicator. Hats denote supplied approximations or
estimates. A prime on a function, $f'$, denotes its derivative; primes on
cable states such as $V'$ denote post-intervention values.
An \emph{oracle} uses an exact field or target-dependent quantity that is
not available to the restricted learner. A seed is one independent draw of
the paired training problem, not one neuron, trial or checkpoint.

Morphology $[b_1,\ldots,b_D]$ lists daughter counts from soma to tips, with
$\sum_{d=1}^D\prod_{r=1}^db_r$ nonsomatic compartments. D1--D4 label physical
stage counts, but not a universal compartment inventory: main Fig.~7 fixes
eight modules, whereas Supplementary Fig.~S9 fixes sixteen terminals.
Feedback budget $K$ counts delivery profiles and is not physical depth.
Lower-case $k$ in a fitted spectrum counts its leading singular directions.

Scalar feedback shares one coefficient \emph{across neurons}. Per-neuron
(or within-neuron) broadcast instead shares each neuron's own coefficient
\emph{within its arbor}; it does not make all neurons receive the same
error. Projected broadcast obtains its coefficient from the exact
compartmental field, unlike somatic broadcast. A local gate changes this
within-arbor delivery using supplied inhibitory activity. The hard,
shunt-proportional and relative-resistance gates are distinct rules,
defined in Section~\ref{note:conductance_learning}.

For the DendriNet selection cohort, $b$ indexes four sensory streams,
$z_{bj}$ is a latent feature, $c_b$ is its one-hot selection cue,
$a_b^{\rm I}=4(1-c_b)$ is inhibitory activity, and $s$ is distractor severity.
$D_p^0$ denotes a proximal compartment's leak-plus-coupling conductance;
$h_p=D_p^0/(D_p^0+g_p^{\rm I}a_b^{\rm I})$ is its relative-resistance gate. These task cues are
distinct from the delivery coefficients $\bm c_u$ in $A_u\bm c_u$.
In the population transport factorization, $\kappa_{ui}$ collects coupling
ratios from terminal $i$ through parent $p$ to soma $u$; it is constant across
examples at fixed weights but changes during learning. The parent sensitivity
$f'_p(V_p)$ equals one for identity outputs and $1-\tanh^2(V_p)$ for
$\tanh$ outputs. The latter is input-dependent. This $\kappa_{ui}$ is distinct
from the focal shunt dose $\kappa_k$ in reciprocal cables.
In main Fig.~9B only, $B$ is the ancestry-block indicator matrix,
$\bm h$ the baseline cable transfer, and $\bm\eta$ the block-gain vector;
this $\bm\eta$ is not the scalar learning rate $\eta$.

NMSE is mean squared prediction error divided by the specified reference.
The algebraic interaction tasks use clean target variance despite noisy
labels; noiseless conductance tasks use the evaluation target variance.
The measured-response prediction study instead divides by the held-out error
of the training-mean predictor (Section~S11). These are not one shared normalization.
Accuracy is a fraction or percent of correctly classified examples; a
percentage-point contrast subtracts two percentages. Capture is a squared-
energy projection ratio in the named field and coordinate system, not an
accuracy, gradient cosine or fraction of gradient surviving context averaging.

\subsection*{Model names and implementations}

DendriNet denotes the PyTorch implementation used for the image, population
selection and physical-depth studies (main Figs.~1, 6 and 7). Its directed
compartments are evaluated from terminals to soma at steady state; additive
controls change the integration rule on the same graph. The seven-compartment
conductance studies (main Fig.~5; Supplementary Figs.~S18--S21) use independent
NumPy implementations of directed current balance. Identity compartment
outputs do not remove conductance division or its nonlinear input dependence.
Neither implementation simulates spikes or membrane-voltage trajectories.

The branch-conflict and ancestry tasks (main Figs.~2--3) use logistic
classifiers whose trees specify feedback supports. The interaction tasks
(main Fig.~4) use algebraic multi-affine scalar trees. Neither uses DendriNet.
The reliability experiment (Supplementary Fig.~S4) uses a separate parallel
conductance model; abstract quadratic and linear-learner checks are identified
in their captions.

Reconstructed anatomy supplies geometry for several distinct analyses.
Main Figs.~8--9 use separate reciprocal steady-state cable solvers;
Supplementary Figs.~S27--S30 analyze anatomical contacts and constructed route
fields. Main Fig.~10 analyzes recorded calcium responses, with a Gaussian
response simulation only for detection power in panel D. The response-prediction
fits in Supplementary Figs.~S33--S34 use reciprocal conductance models on
reconstructed trees and separate linear-regression baselines. These fits are
models of measured responses, not DendriNet experiments or recordings of
learning signals. Captions distinguish the model or analysis in mixed figures.
